#include <stdio.h>
#include <iostream>

#include "Pathfinding.h"
#include "ECS.h"

Position Pathfinding::GetPath(Position a, Position b, std::shared_ptr<Map> map) {
	//Calculate first step in path from a to b on given map
	return a;
}